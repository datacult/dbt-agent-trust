# Python tooling for this repo (agent, evaluation, MCP).
# dbt is NOT here — Fusion is a standalone binary.

duckdb
pydantic-ai-slim[anthropic,openai,mcp]
mcp               
python-dotenv     
pyyaml            